## Question 02

`hello.c` 中的用户态示例代码会加载 `hello.bpf.o` 中定义的所有程序对象。作为编写 `libbpf` 用户态代码的练习，请修改该示例代码，使其仅加载并挂载其中一个 eBPF 程序（任选其一即可），且无需从 `hello.bpf.c` 中删除其他程序的代码。

## Answer

1. 代码修改

```diff
--- a/chapter7/hello.c
+++ b/chapter7/hello.c
@@ -63,10 +63,12 @@ int main()
                return 1;
        }

-       // Attach the progams to the events
-       err = hello_bpf__attach(skel);
-       if (err) {
-               fprintf(stderr, "Failed to attach BPF skeleton: %d\n", err);
+       struct bpf_link *link;
+
+       // Attach only the kprobe_sys_execve program
+       link = bpf_program__attach(skel->progs.kprobe_sys_execve);
+       if (!link) {
+               fprintf(stderr, "Failed to attach kprobe_sys_execve program\n");
                hello_bpf__destroy(skel);
         return 1;
        }
```

2. 编译运行